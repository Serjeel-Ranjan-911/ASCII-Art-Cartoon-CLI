import gradient from "gradient-string";

export let gradientsOptions = [
	"cristal",
	"teen",
	"mind",
	"morning",
	"vice",
	"passion",
	"fruit",
	"instagram",
	"atlas",
	"retro",
	"summer",
	"pastel",
];

const cristal = (str) => {
	return gradient.cristal(str);
};

const teen = (str) => {
	return gradient.teen(str);
};

const mind = (str) => {
	return gradient.mind(str);
};

const morning = (str) => {
	return gradient.morning(str);
};

const vice = (str) => {
	return gradient.vice(str);
};

const passion = (str) => {
	return gradient.passion(str);
};

const fruit = (str) => {
	return gradient.fruit(str);
};

const instagram = (str) => {
	return gradient.instagram(str);
};

const atlas = (str) => {
	return gradient.atlas(str);
};

const retro = (str) => {
	return gradient.retro(str);
};

const summer = (str) => {
	return gradient.summer(str);
};

const pastel = (str) => {
	return gradient.pastel(str);
};

export default {
	cristal,
	teen,
	mind,
	morning,
	vice,
	passion,
	fruit,
	instagram,
	atlas,
	retro,
	summer,
	pastel,
};
